================================================================
AD4134 Continuous Streaming - SIMPLE BUILD (Recommended)
================================================================

STEP 1: Add ONLY the streaming library to src.mk
================================================================

Edit: /home/neutrino/work/no-OS/projects/ad4134_fmcz/src.mk

Add this line after "SRC_DIRS += $(DRIVERS)/adc/ad713x" (around line 13):

-------- ADD THIS TO src.mk --------
# Continuous streaming library
SRCS += $(PROJECT)/src/ad4134_continuous_streaming.c

# Keep using the original main (ad713x_fmc.c is already included via SRC_DIRS)
------------------------------------

NOTE: Do NOT add ad713x_fmc_streaming_example.c yet - it needs API updates
      for your specific no-OS version.

STEP 2: Build
================================================================

cd /home/neutrino/work/no-OS/projects/ad4134_fmcz
make clean
make PLATFORM=xilinx TARGET=zed

This will compile:
- ad4134_continuous_streaming.c (NEW library)
- ad713x_fmc.c (EXISTING main - unmodified)

STEP 3: Warnings are OK
================================================================

You will see format warnings like:
  warning: format '%u' expects argument of type 'unsigned int',
  but argument 2 has type 'uint32_t'

These are harmless - just printf format mismatches between platforms.
The code will work correctly.

STEP 4: Next Steps - Integrate into your code
================================================================

Once the library compiles, you can integrate continuous streaming
into your existing ad713x_fmc.c:

1. Add header:
   #include "ad4134_continuous_streaming.h"

2. Replace the while(1) loop (around line 396-428) with streaming calls

3. See INTEGRATION_EXAMPLE.txt for code snippets

================================================================
STATUS: Library-only build should complete successfully
================================================================

If you get errors about 'no_os_print_log.h', the fixes have already
been applied to use standard printf() instead.

For detailed integration instructions, see:
- CONTINUOUS_STREAMING_README.md (full API docs)
- INTEGRATION_EXAMPLE.txt (code snippets)
================================================================
